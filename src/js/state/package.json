{
  "name": "state",
  "version": "1.0.0",
  "description": "Javascript used for State pages",
  "main": "index.js",
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browserify": "browserify index.js | uglifyjs -mc > ../../../dist/js/state.js",
    "watchify": "watchify index.js -o ../../../dist/js/state.js -dv",
    "start": "npm run watchify",
    "build": "npm run browserify"
  },
  "author": "Roy Hewitt <royhewitt07@gmail.com>",
  "license": "SAX-PD",
  "dependencies": {
    "query-string": "^4.3.4",
    "xhr": "^2.2.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^13.0.0",
    "uglifyjs": "^2.4.10",
    "watchify": "^3.7.0"
  }
}
