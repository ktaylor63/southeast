{
  "name": "hero-focal-point",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "babel-preset-env"
          ]
        }
      ]
    ]
  },
  "scripts": {
    "browserify": "browserify index.js | uglifyjs -mc comparisons=false > ../../../site/static/js/hero-focal-point.js",
    "watchify": "watchify index.js -o ../../../site/static/js/hero-focal-point.js -dv",
    "inspect": "browserify --full-paths index.js | uglifyjs -mc comparisons=false | discify --open",
    "start": "npm run watchify",
    "build": "npm run browserify"
  },
  "keywords": [],
  "author": "@r0yhewitt <roy_hewitt@fws.gov>",
  "license": "SAX-PD",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "browserify": "^15.2.0",
    "uglify-js": "^3.3.9",
    "watchify": "^3.10.0"
  },
  "dependencies": {
    "draggable": "^4.1.3",
    "string-to-dom": "^0.1.5"
  }
}
